# Freebuff-local state (run docs, preview logs)
.freebuff/

# OS/editor junk
.DS_Store
Thumbs.db
*.log

# Secrets - never commit the real deployment env
.env

# Deployed runtime state (reports, users.db, tokens, site configs)
data/
